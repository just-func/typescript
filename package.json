{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "verify": "turbo run verify"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "turbo": "latest"
  }
}